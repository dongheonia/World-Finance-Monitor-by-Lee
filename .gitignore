# No longer loaded by index.html (FMP is unused now — see market-data.js) — kept
# ignored in case a local js/config.js still exists from before, so it never gets
# committed by accident.
js/config.js

# Python
.venv/
__pycache__/
*.pyc

# OS / editor
.DS_Store
.claude/
